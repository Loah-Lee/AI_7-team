# RAG 파이프라인 수동 실행 가이드

이 문서는 `data` 폴더 내의 원본 문서들을 처리하여 하이브리드 검색이 가능한 RAG 데이터베이스를 구축하는 전체 파이프라인 과정을 안내합니다. AI에 의해 자동 작성되었습니다.

## 0. 사전 준비

모든 작업은 본 브랜치의 requirements.txt를 통해 필요한 패키지들이 설치된 환경에서 수행해야 합니다.

이후의 모든 명령어는 해당 환경이 활성화된 상태에서 실행해야 합니다.

또한, libreoffice를 활용하고 있으며 브랜치에 함께 업로드한 H2Orestart-0.7.7.oxt를 설치해야 정상적으로 PDF 변환이 이뤄집니다.

## 1. [1단계] 문서 준비 및 HWP 변환

이 단계에서는 `data` 폴더의 문서들을 처리 가능한 PDF 형태로 `output/temp_pdf` 폴더에 준비합니다.

1.  **HWP 파일을 PDF로 변환**: `data` 폴더에 `.hwp` 또는 `.hwpx` 파일이 있는 경우, `hwp_converter.py` 스크립트를 사용하여 PDF로 변환합니다. 각 HWP 파일에 대해 아래 명령을 실행하십시오.

    ```bash
    # 예시: python3 hwp_converter.py <입력 HWP 파일 경로> -o <출력 디렉토리>
    python3 hwp_converter.py data/your_document.hwp -o output/temp_pdf
    ```

2.  **PDF 파일 이동**: `data` 폴더에 이미 PDF 파일이 있다면, `output/temp_pdf` 폴더로 이동시킵니다.

**[완료 조건]**: 모든 원본 문서들이 `output/temp_pdf` 폴더 안에 PDF 파일 형태로 준비되어 있어야 합니다.

## 2. [2단계] PDF 파싱 (PDF → Markdown)

`parser_step1.py` 스크립트를 실행하여 `output/temp_pdf` 폴더의 모든 PDF 파일을 마크다운(.md) 텍스트 파일로 변환합니다.

```bash
python3 parser_step1.py
```

-   **입력**: `output/temp_pdf/` 폴더의 PDF 파일들
-   **출력**: `output/` 폴더에 `step1_parsed_*.md` 형태로 생성된 마크다운 파일들

## 3. [3단계] 검수 및 메타데이터 추출

`auditor_reader_step23.py` 스크립트를 실행하여 파싱된 마크다운 파일의 오류를 교정하고, 문서 제목과 같은 메타데이터를 추출하여 파일 상단에 추가합니다.

```bash
python3 auditor_reader_step23.py
```

-   **입력**: `output/` 폴더의 `step1_parsed_*.md` 파일들
-   **출력**: `output/` 폴더에 `step3_final_*.md` 형태로 생성된 최종 마크다운 파일들

## 4. [4단계] 의미론적 분할 (Chunking)

`chunker_step4.py` 스크립트를 실행하여 최종 마크다운 파일을 검색에 용이한 작은 단위(청크)로 분할하고, 각 청크에 상위 목차와 같은 메타데이터를 포함시킵니다.

```bash
python3 chunker_step4.py
```

-   **입력**: `output/` 폴더의 `step3_final_*.md` 파일들
-   **출력**: `output/chunks/` 폴더 내에 다수의 `chunk_*.json` 파일들

## 5. [5단계] 데이터베이스 구축 (Storage & Indexing)

`storage_step5.py` 스크립트를 실행하여 모든 청크 데이터를 로드하고, 벡터 검색(Dense)과 키워드 검색(Sparse)이 모두 가능한 하이브리드 데이터베이스를 생성합니다.

```bash
python3 storage_step5.py
```

-   **입력**: `output/chunks/` 폴더의 JSON 청크 파일들
-   **출력**: 프로젝트 루트의 `DB/` 폴더에 생성된 `document.db` 파일

---

위의 5단계를 순서대로 모두 수행하면 RAG 애플리케이션에서 사용할 수 있는 최종 데이터베이스 구축이 완료됩니다.

---

## 6. 새로운 문서 추가 시 고려사항

이 파이프라인은 범용적으로 설계되었으나, 문서의 구조와 형식의 다양성으로 인해 새로운 문서를 처리할 때 다음과 같은 문제들이 발생할 수 있습니다.

1.  **HWP 변환 품질**:
    *   `libreoffice`를 이용한 변환은 대부분의 HWP 파일에서 잘 작동하지만, 매우 복잡한 표나 특수한 서식을 포함한 문서의 경우 PDF 레이아웃이 일부 깨질 수 있습니다.
    *   **한글 폰트**: 시스템에 한글 폰트가 설치되어 있지 않으면 PDF 변환 시 한글이 깨져 보일 수 있습니다.

2.  **PDF 파싱 정확도**:
    *   **스캔 문서**: 텍스트가 아닌 이미지로 이루어진 스캔 PDF는 텍스트를 추출할 수 없어 파싱이 불가능합니다.
    *   **복잡한 레이아웃**: 다단(multi-column) 편집, 비정형적인 표, 텍스트 상자가 많은 디자인의 문서에서는 텍스트 순서가 뒤섞이거나 표 데이터가 누락될 수 있습니다.

3.  **메타데이터 및 헤더 추출**:
    *   **문서 제목**: `auditor_reader_step23.py`는 첫 페이지에서 가장 큰 폰트의 텍스트를 제목으로 간주합니다. 문서 표지의 디자인이 특이할 경우(예: 제목보다 큰 로고 텍스트), 다른 내용이 제목으로 잘못 추출될 수 있습니다.
    *   **헤더 인식**: 헤더(목차)를 폰트 크기 기준으로 판단하므로, 본문 내용 중 강조된 일부 텍스트가 헤더로 잘못 인식되거나, 실제 헤더가 누락될 수 있습니다.

### 권장 사항

-   **개별 테스트**: 새로운 문서는 한 번에 하나씩 파이프라인을 실행하며 각 단계의 결과물(`step1_parsed...`, `step3_final...` 등)을 확인하는 것이 안전합니다.
-   **스크립트 미세 조정**: 특정 문서에서 만족스러운 결과가 나오지 않는다면, 원인에 따라 파이셔 스크립트의 일부 파라미터(예: `auditor_reader_step23.py`의 헤더 판단 폰트 크기 기준)를 수정해야 할 수 있습니다.
